const Header = (): React.ReactElement => {
  return (
    <img
      src="images/logo.svg"
      alt="Grandma's Cookbook's logo"
      width="200"
      height="63"
    />
  );
};

export default Header;
